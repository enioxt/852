#!/bin/sh
# 852 Pre-Commit — delegates to universal CRCDM hook
#
# CANONICAL HOOK: ~/.egos/hooks/pre-commit (symlinked via .git/hooks/pre-commit)
# This file exists for documentation; it is NOT the active hook.
#
# Active hook chain:
#   git → .git/hooks/pre-commit → ~/.egos/hooks/pre-commit
#
# The active hook provides:
#   - gitleaks secret scan (blocking)
#   - Supabase PAT / GitHub PAT regex fallback (blocking)
#   - Doc proliferation check (blocking)
#   - SSOT file size limits (warning)
#   - Handoff archive freshness (warning)
#   - CRCDM DAG node + observability logging
#
# To activate this file as a HUSKY hook instead:
#   npm install husky --save-dev
#   npx husky init
#   (husky will set core.hooksPath = .husky/_)
#
# Decision (852-GOV-201, 2026-03-29):
#   Single canonical hook = ~/.egos/hooks/pre-commit
#   No dual-hook ambiguity. CRCDM + governance checks merged.
echo "⚠️  .husky/pre-commit called directly — active hook is .git/hooks/pre-commit"
exit 0
