# EGOS-KERNEL-PROPAGATED: 2026-04-08
# Extends: EGOS Kernel (~/.claude/CLAUDE.md) — kernel wins on conflict.

# .windsurfrules — EGOSv3 Production Workspace
# VERSION 5.4.0
# MAX_LINES: 150 (HARD LIMIT)

---
sacred_code: "000.111.369.963.1618"
version: "5.4.0"
project: "852-Inteligencia"
architecture: "MCP-FIRST + NEXUS-ZERO + INTELINK + RHO"
---

## ⚡ PRIME DIRECTIVE

> **NEVER write manual logic if a Tool (MCP) exists.**
> **ALWAYS read the appropriate guide before complex actions.**
> **CHALLENGE before EXECUTE. Process before code.**

## 🎯 MANDAMENTOS (ABSOLUTOS)

1. **START** → Execute `/start` antes de qualquer trabalho
2. **READ** → "Já Concluído" = NÃO REIMPLEMENTAR
3. **SSOT** → Tasks APENAS em `TASKS.md` (root)
4. **THINK** → Sequential Thinking (P0=7, P1=5, P2=3 thoughts)
5. **COMMIT** → Convencional (feat:/fix:/chore:) cada 30-60min
6. **END** → Execute `/end` ao finalizar
7. **ONE THING** → Cada agente faz UMA coisa.

## 📂 GUIAS

- **MCPs:** `/home/enio/egos-lab/.guarani/MCP_ORCHESTRATION_GUIDE.md`
- **Código:** `.guarani/PREFERENCES.md`
- **Shared EGOS:** `.egos/guarani/PREFERENCES_SHARED.md`
- **System Map:** `.egos/SYSTEM_MAP.md`, `SYSTEM_MAP.md` ou `docs/SYSTEM_MAP.md` (ler primeiro se existir)
- **Design Standards:** `/home/enio/egos-lab/.guarani/DESIGN_STANDARDS.md`
- **Orquestração:** `.egos/guarani/orchestration/PIPELINE.md`

## 🚀 DEPLOY & INFRAESTRUTURA

- **App:** Next.js 16 + Turbopack + TailwindCSS
- **Database:** Supabase PostgreSQL
- **AI/LLM:** Alibaba qwen-plus via DashScope / Gemini via OpenRouter
- **Deploy:** Contabo VPS + Docker Compose + Caddy (`852.egos.ia.br`)

## 🎨 DESIGN CONSTITUTION (ENTERPRISE)

1. **Identity:** "Police Intelligence" (Palantir/Linear style). Dark Mode Only.
2. **Spacing:** Tailwind 4pt grid (`gap-4`, `p-6`). NO magic numbers.
3. **Fitts's Law:** Hit-area min 40x40px. Padding INSIDE links.
4. **Symmetry:** Inputs/Buttons share height (`h-10` or `h-12`).
5. **Colors:** 🔴Danger 🟠Warn 🟢Safe 🔵Action ⚪Text ⚫Structure.

## 🚨 REGRAS CRÍTICAS

### Privacy & PII
Agents MUST mask CPF/email, specific names, and process numbers in all output.

### Deploy Contract
`docker-compose.yml` (repo root) é SSOT do runtime no Contabo.
Deploy por rsync DEVE excluir symlinks/artefatos locais do mesh EGOS.
Se o Caddy fizer proxy por nome de container, o app DEVE compartilhar a rede reversa correta.

### Activation Contract
`/start` DEVE carregar handoff, TASKS, System Map, module roots e deploy surface antes de mudanças amplas.

### Timeout & Fallback Contract
Pesquisas externas e MCPs potencialmente lentos DEVEM começar com escopo reduzido, poucas respostas e fallback explícito.
Se uma ferramenta travar, demorar além do razoável ou ficar sem retorno útil, o agente DEVE interromper cedo, informar o usuário e trocar para leitura local, consulta menor ou fonte alternativa.
Nunca bloquear a sessão indefinidamente aguardando pesquisa longa.

### SSOT Tasks
```text
TASKS.md (root)     ← ÚNICO local para tasks
├── P0 = Blockers (máx 3)
├── P1 = Sprint (máx 5)
├── P2 = Backlog
└── MÁX 500 linhas
```

## 📋 DOCUMENTATION DISCIPLINE (MANDATORY)

### Anti-Proliferation Rules

1. **NO TIMESTAMPED DOCS** — NEVER create `*_2026-03-*.md`, `*AUDIT_2026*.md`, `*REPORT_2026*.md`
2. **UPDATE SSOT, DON'T CREATE** — Audits/diagnostics MUST update `AGENTS.md`, `TASKS.md`, `docs/SYSTEM_MAP.md`
3. **HANDOFFS ARE EPHEMERAL** — Consolidate in `docs/knowledge/HARVEST.md`, archive >30 days
4. **ONE SSOT PER CONCERN** — `AGENTS.md` = capabilities, `TASKS.md` = backlog, `SYSTEM_MAP.md` = architecture
5. **KERNEL CANONICAL ONLY** — `SSOT_REGISTRY.md`, `CAPABILITY_REGISTRY.md`, `CHATBOT_SSOT.md` live in `/home/enio/egos/docs/`

### Allowed Docs

- `AGENTS.md`, `TASKS.md`, `README.md`, `docs/SYSTEM_MAP.md`, `docs/_generated/*`, `docs/_archived_handoffs/YYYY-MM/*` ✅

### Prohibited

- ❌ `docs/*AUDIT*.md`, `docs/*DIAGNOSTIC*.md`, `docs/*REPORT*.md` with date stamps (except `_archived/`)
